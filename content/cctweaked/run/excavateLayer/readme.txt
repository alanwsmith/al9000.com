- TODO: Excevates a layer that's
three blocks tall. 

- Start the turtle on the middle
height of the three blocks at the
1x1 position on an inside corner
of the chunk.

- The turtle moves forward 
and right as it clears the 
chunk.

- The `ejectExtras` function
is run every other row so 
that only one stack of a 
given type is retained
(excluding any allowances
from the command)

- Torches are laid if available

